# Track Registry

The control plane — every track, its state, and where to resume. **Start here.**
Maintained by the `create` and `handoff` workflows.

## Active

| ID | Track | Next action | Worktree | Updated |
|----|-------|-------------|----------|---------|
| [018](018-reservation-state-machine.md) | Reservation state machine — explicit model + matrix enforcement | **P0 DONE — de facto machine extracted** (`018-state-machine-defacto.md`: 7 state components, kinds K1–K7, full transition inventory, divergences D1–D13 incl. B1 split non-conservation ×3 paths, kind-blind guards from `paid-in-cash` overloading, delete-on-abandon of occupied seats). **P1 review held — all forks decided** (till partition on split · credit notes for cash refunds · money-rows-never-deleted rule · departed re-seatable same day); intended model drafted in `018-state-machine-intended.md` (state tuple + I1–I7 invariants + full transition table, reject-by-omission). **P1 SIGNED OFF; ▶ P2 — slices 1+2 shipped:** pure model (`@repo/data/reservation-machine`, committed `608bc55`) + server interpreter (`reservation-machine-apply`: applyTransition with fact-computed conditions, I4 delete defense, till-partitioning split — B1 end-to-end regression green; 15 integration tests, data 318u+320i, uncommitted). Slice (b) meta-guard shipped: single-writer ratchet over all apps (36 legacy sites / 9 files, exact-equality shrink-only allowlist) — **scan discovered 2 writer files P0 missed** (frontdesk fossil D14, partner reservation-detail D15). **P4 slices 1 (`4c386e4`) + 2 (uncommitted) done.** Slice 1: all six red cells GREEN (B1 impossible by construction; ratchet 19→13; the money-inventing split recompute killed). Slice 2: **D14/D15 fossils migrated** (frontdesk regains the multiday cycle; parent-only writes deleted; allowlist 9→7 files), **new `undoDepartWalkIn`** (same-day undo + conflict recheck; matrix 19 cells), **cron structurally can't delete money rows** (I4 filter). Partner 1966u+198i, data 320u+327i. **Slice 3 (UI) shipped (uncommitted):** bed-state = presentation shell over deriveState (grid + guards share ONE derivation); ↩ Undo-departure button in the FREE panel; unreserve dialog shows the truth (Seat → partitioned share, Group → actual till total). 2 i18n keys × 3 locales (**ES/FI await founder copy review**). Slice 3 committed `062f6e6` + **BROWSER-VERIFIED end-to-end** (partitioned €8.50 dialog, undo-departure re-seat, first real credit note `PARTNER-CN-2026-00001`, till void+recreate trail — screenshots + DB proof, rows cleaned). **Slice (e) shipped (uncommitted): collect flow on the machine — D5 + D6 dead at the action level** (settled walk-ins machine-rejected; abandon can never free a bed — paid race honestly resolves as pay.confirm; provider failures revert to unsettled cash). Interpreter gained the payment-rail executors (amountFromDb/mintAnonId/mollieCreate/reverifyOnce/mollieCancel/invoiceOnline); ratchet 13→8. **BUILT SCOPE COMPLETE (P0–P4, one day, 10 commits `608bc55`…HEAD; slice g uncommitted).** The signed transition table + interpreter is the ONLY reservation state writer (allowlist 36 sites/9 files → 9/2); B1 double-charge family + D5/D6/D14/D15 fixed by construction and browser-verified (first live credit note `PARTNER-CN-2026-00001`); wiki page `subsystems/reservation-state-machine.md` + CLAUDE.md tree synced. **Pushed to `main` 2026-08-11 (`be954d4`, migrations live on test DB). Founder op: ES/FI copy review before promote (4 keys).** Backlog: manage create-verbs batch (8→0), rentals/tables machines on the same rails, reservation-day.ts unification. | — | 2026-08-11 |
| [016](016-day-anchored-till.md) | Day-anchored till (per-day till the operator expects) | **FUNCTIONALLY COMPLETE & browser-verified (committed 2026-07-23: 'Day-anchor the per-worker till', local main).** Till reworked to day-anchored two-bucket windows (today + amber carry-over), sweep-together close via shared `closeEmployeeTill` (snapshot records carry-over), daily summary + day close lead with close-independent daily accumulation. data 265u+289i, partner 1897u+179i green. **Next (user ops): `migrate:test` before `main` push (hook enforces); founder copy-review of new en/es/fi keys before promote.** | — | 2026-07-23 |
| [013](013-settlement-ledger-till.md) | Settlement ledger — offline cash till source | **Building (P0a).** Replace the operational-status-derived till with an explicit cash-settlement ledger (`TillEntry`): a "Settle" cash event (alternative to card "Collect payment") is the till source, decoupled from check-in churn / seat turnover. Walk-in just occupies; Rent→Walk-in/Check-in rename; refund = "money returned" checkbox voids the settlement. Additive schema + cross-app. | — | 2026-06-21 |
| [001](001-knowledge-store.md) | Knowledge Store | Phase 1 — stand up Vercel test-env log-drain adapter | — | 2026-05-20 |
| [002](002-table-reservations.md) | Table Reservations | P1 complete + fully shipped (`71c75e7` on all branches). **▶ P1.5 QR order & pay at the table (dine-in tabs)** — design locked 2026-07-18 (tab opens on first order; v1 on site Order/Product rails; pay-at-end, one payment grouped by `paymentRef`). **Phases 2+3 DONE, green & committed (`b55f0b8` data + `f628d7a` user, local `main`, unpushed)** — data layer (`TableTab` + `openTableId` guard + `calculateTabTotal`/`processConfirmedTabPayment`) + user-app ordering (`/sites/[id]/dine/[tableId]` menu → `placeTabOrder` rounds → running tab; 28 `Dine` i18n keys await founder copy-review). data 265u+268i, user 398u+70i green; migration applied local+`sunbnb_test`, **`migrate:test` pending before `main` push** (pre-push hook enforces). **Next after reboot: phase 4 tab payment** ("Close & pay" + Mollie route + webhook branch + demo) | — | 2026-07-18 |
| [004](004-partner-test-architecture.md) | Partner Test Architecture | **PARTNER SCOPE COMPLETE** (Phases 0/3/1/4 — spine, fixes, backfill, cleanup+coverage-ratchet; 901 unit + 89 integration green, full gate green). Phase 1+4 local (`d4621b0`→`8386e7b`, 6 unpushed). Deferred: @repo/test-utils extraction, createWalkInRental race. | — | 2026-06-17 |
| [006](006-alonso-staff-ui.md) | Alonso → Staff UI Migration | **⚠ REOPENED 2026-07-09 — customer "literal parity" feedback.** Floor-state half stays at parity (P1–P8 + multiselect + refund + dark/zoom, live). Analysis found **three reporting/attribution *presentation* gaps** (plumbing exists, views missing) = new **P9**: (1) no today-revenue / total-rented hero on the manage floor; (2) till is aggregate-only — no per-seat+time itemized list, single-worker, cash-scoped vs the wanted full rental attribution; (3) accounting lacks a per-day *table* + surfaces invoice-count not sunbeds-rented/day. Cross into [007](007-operator-analytics.md) (gap 3) + [008](008-employee-model.md) (gap 2). **P9 BUILT 2026-07-10 (uncommitted, tests green):** all three gaps landed on the **accounting tab** (desktop, session-gated) on a **takings** basis — `@repo/data` `getReservationDayStats`/`getEmployeeShiftItems` (additive, no migration) → `getOperationsTrend`/`getStaffShiftItems` actions → Hoy card + *Historial detallado* table + expandable staff-till itemization. Data 241u+172i, partner 1716u green. **Next: browser-verify + commit; mobile staff-phone surfacing deferred.** | — | 2026-07-10 |
| [010](010-floor-reservation-lookup.md) | Floor Reservation Lookup & Arrivals — "Guests" host stand | **P1+P2 SHIPPED & LIVE on test+prod** (`e7952fd`). `findReservations` + the Guests bottom sheet (arrivals + name/phone/email search beyond today's grid, 🔍 anchor FAB, Locate→`BedDetail`, inline Check-in/Rent). **Floor-lookup gap vs Alonso closed end-to-end.** P3 **correctness slice** (error/empty/loading/a11y) shipped (`80bacc5`). **Next action: VALIDATE, don't build — the surpass layer (P3 roster-CRM, P4 scan-the-pass, P5 resell) is parked as unvalidated demand (Alonso-model hypotheses, not operator asks). Instrument the hypotheses (pass-QR usage, unfulfilled-hold rate) or wait for an operator signal before building.** | — | 2026-06-20 |

| [015](015-cash-sale-receipts.md) | Cash-sale receipts (invoice every income) | **ALL 5 PHASES BUILT & GREEN; backfill applied to test+prod (38 prod receipts).** Cash sales now get a PARTNER receipt (receipt-only, no commission), reusing the invoice core — P1 core (`skipCommission`/`invoicedAt`/`skipEmail` opts) · P2 rentals/orders · P3 wired at *settle* (guard=`recordCashSettlement`) · P4 env-tiered idempotent backfill · P5 **accounting-export UI** (fiscal summary + VAT-by-rate + CSV invoice register, replaced the detail tables). data 247u+219i, partner 1725u green. **Next (user ops): commit + deploy, then re-run `backfill:cash:production` to sweep the deploy gap.** Deferred: cash refund→credit-note. | — | 2026-07-10 |

## Proposed / backlog

| ID | Track | Why deferred | Updated |
|----|-------|--------------|---------|
| [019](019-hw-api.md) | HW API — device state endpoint + telemetry | **Scoped, ready to build — not deferred, waiting on hardware in transit.** Serves the parasol-mounted device (sibling repo `../sunbnb-hw`, design converged over 9 ADRs) from this backend with **no new infrastructure**: `GET /api/hw/{code}/state` + `POST …/telemetry`, polled directly over HTTPS by an ESP32-C6 (ADR 0008 deleted the broker/worker/gateway layer the earlier LoRa design implied). **Renamed off "beacon" 2026-08-13** — the device is a Wi-Fi *polling client* with an LED bar, not a broadcaster, and the name had leaked into a fielded, effectively unrenameable surface; `/api/hw/` names the client class, so a hardware or branding pivot never touches a potted URL. Wire contract drafted (4 closed states FREE/RESERVED/OCCUPIED/UNAVAILABLE, per-seat array + aggregate, **frozen seat order**, server-driven `pollAfterSec`, `cmd: stow`, **uniform 401** — the drafted 401-vs-404 split was an enumeration oracle against a code printed on a public beach) — **not frozen; cheap to change until devices are potted, expensive after**. Hard constraint: the route is a **second shell over `deriveState`** ([[track:018]]) — never a second opinion on seat state; fail-safe direction is *never resolve doubt toward FREE* (a wrong FREE double-sells a lounger). **Identity decided (Q2):** public 6-char Crockford-base32 code (server-issued, random) + secret 32-byte per-device token stored sha256-only; MAC-derived and sequential codes rejected. **Q1 DECIDED: explicit seat list** (`DeviceSeat` join table, `position` = mount order) — the binding is a *physical installation* fact and `SunbedGroup` is a *booking* fact; deriving one from the other over-scopes wherever grouping ≠ shading and lets an inventory edit silently change what a light means. **P1 CODE COMPLETE (uncommitted): `apps/user/app/api/hw/[code]/state/route.ts` + 43 route tests, user 521u green, tsc/lint clean.** Non-obvious finding baked in: the route must NOT fall back to the parent `operationalStatus` when today's `ReservationDay` row is absent (the manage page never hits this — it lazy-upserts; a device polls first, and upsert-on-read would be ~1.3 M writes/day), else a multiday guest who checked in yesterday reads OCCUPIED instead of RESERVED. **Next action: bring-up when the kit lands** — set `HW_TOKEN`/`HW_DEVICE_MAP` on test, curl the route, point the device; then P2 `Device`+`DeviceSeat` migration and **freeze the wire contract** (Q6 check character + Q7 token rotation must be answered first). **Device management split into three surfaces with three owners** (2026-08-13 restructure — the old roadmap conflated them): P3 **minting** = platform-side bench *script*, not a UI; P4 **binding** = operator-side *field* flow on the mobile/token-gated `/sites/[id]/manage` (binding happens at the parasol, not at a desk; needs `cmd: "identify"` to self-verify) — the surface Q1's explicit seat list makes necessary; P5 telemetry persistence + operator health list. Optional P1.5 telemetry stub for firmware bring-up. 💤 P6 fleet scale (Q3: ~1.3 M invocations/day at 1 500 devices). None of P3–P5 is needed for the demo — they start at the second and third unit. **Scope narrowed to the device↔API link; parked:** guest QR entry route + seat-state unification (the latter records a real POS bug — an unfiltered `reservations` include makes *canceled* bookings read as "not available"). | 2026-08-13 |
| [017](017-venue-timezone-anchoring.md) | Venue timezone anchoring — one civil-day convention platform-wide | **P1–P5 + P7 DONE — only P6 (structural) left. P7 done 2026-08-13 (uncommitted, local `main`)** — `Site.timeZone` now auto-derived on save + backfill script + restaurant tz inheritance; closes the P2 availability residual (live dry-run found 3 Europe/Athens sites mis-anchored to Madrid). data 340u, partner 1973u+200i, `turbo build` 4/4; no migration. **P3–P5 committed unpushed. User ops: push/deploy + run `backfill:tz:*`. Next: P6 (collapse tz modules, needs Q4).** **P1+P2 pushed 2026-08-11.** — analytics day bucketing venue-anchored (occupancy double-count / >100% fixed, regression proven) + rolling-trend windows (deferred from P4). data 335u+350i, partner 1970u, `turbo build` 4/4; no migration. **P3+P4 committed unpushed (`a8fbc07`…`592490f`). Next: commit P5; then only robustness left — P7 (Site.timeZone, closes Canary residual) / P6 (collapse tz modules, needs Q4).** **P1+P2 pushed 2026-08-11.** — `siteMonthBounds` primitive + partner accounting month/day windows venue-anchored (fiscal/VAT month boundaries) + DailySummaryView "today" fixed; site-level scope (rolling trends fold into P5, account-level `getInvoicesByMonth` deferred). partner 1970u, data 335u, `turbo build` 4/4; no migration. **P3 committed unpushed (`a8fbc07`/`889d33e`/`3850fc6`). Next: commit P4; then P5 (analytics bucketing + rolling trends) / P7 (Site.timeZone) / P6.** **P1+P2 pushed 2026-08-11.** — venue-anchored both reservation WRITE paths (Q1=(a), Q2=fix-forward): `siteAnchoredDay` primitive + consumer `saveReservationForMultipleItems` + partner `createPartnerReservation`/`getAvailableSunbeds`; also fixed a latent west-of-UTC bug in `localMidnight`. data 329u+349i, user 521u+78i, partner 1968u+199i, `turbo build` 4/4; no migration. **Next: commit P3; then P7 (Site.timeZone, closes Canary residual) / P4 (VAT month windows) / P5 / P6.** P1+P2 pushed 2026-08-11. **Scoped from an audit.** P1: venue-anchored settlement window (`siteDateBounds` + `resolveSettlementWindow`, `f0b51ba`). P2: venue-anchored reminder-email + availability windows (data reminders + apps/user availability, 2 commits). data 281u+305i, user 478u+77i green; no migration. **Next: resolve Q1/Q2 to unblock P3 (write-path root cause); P7 (populate `Site.timeZone`) is independent + closes the P2 Canary residual.** **Scoped from an audit.** [012](012-multiday-per-day-operational-state.md) P0 built the correct primitive (`site-day.ts`, DST-aware) and added `Site.timeZone`, but it was adopted in **9 files only** (all `manage/` + `till.ts`). Everything else does day math in the **server** TZ (UTC on Vercel) or the **browser's** — so `Reservation.from/to` now holds **two conventions**, making readers wrong rather than merely shifted. Verified: reminder emails can be **permanently missed** (`reservation-emails.ts:390`); consumer availability **undercounts** free sunbeds (`siteService.ts:145`); settlements **drop their final day** (`settlement.ts:99` `lt: periodEnd`); `Site.timeZone` is a **dead column** (never written); a linked restaurant **never inherits** its site's tz; `table-reservations-core` reinvents civil days as `+24h` (DST-broken). **Next: resolve Q1 (what anchors a consumer-written booking) + Q2 (backfill?); or ship P1 (settlement final-day — isolated, money, 2 files) first, which needs neither.** | 2026-08-11 |
| [011](011-group-multiselect-reservation.md) | Group multiselect Reserve/Rent into one reservation | **Scoped, ready to build (awaiting go-ahead).** Partner manage multiselect Reserve/Rent currently makes one reservation PER SEAT — N bookings for one party on N loungers. Fix: a grouped create over the existing `reserveWithConflictGuard({ itemIds })` primitive (already used by the consumer flow + pair-expansion) so a multiselect = one reservation. **Decisions locked: always group (no toggle) + all-or-nothing (one taken seat fails the group).** Small, partner-only, **no schema/migration** (Reservation↔items already many-to-many; downstream already handles multi-item reservations). **Next: P1 — grouped action(s) + gated-actions/auth-matrix registration.** | 2026-06-20 |
| [012](012-multiday-per-day-operational-state.md) | Per-day operational state for multiday sunbed reservations | **Scoped + designed (awaiting decisions before P0).** Multiday bookings carry ONE operational state for the whole stay → stale day-1 arrival time, no daily expected→arrived reset, and a **day-2 double-sell** (departing day-1 frees the bed via `bed-state` `departed` filter). Fix: per-day `ReservationDay` rows (status/checkedInAt/departedAt per civil date); manage/frontdesk read today's row. **Schema change** (additive `ReservationDay` + `Site.timeZone` — no tz field exists today; `Restaurant.timeZone` is precedent). Bug is **duplicated in `frontdesk/`** (P3). Availability stays whole-stay (P2 invariant). **Next: confirm Open decisions Q1–Q5, then P0 expand migration.** | 2026-06-20 |
| [003](003-stripe-connect-compliance.md) | Stripe Connect compliance | **Green-field — consumer Stripe code removed (2026-05-23).** Stripe is subs-only; consumer = Mollie + Demo. Build on Connect from scratch if/when consumer Stripe is reintroduced. | 2026-05-23 |

## Done / archived

| ID | Track | Outcome | Updated |
|----|-------|---------|---------|
| [014](014-reserve-first-site-view.md) | Reserve-first site view (user app) | **Done & user-verified (local commit, unpushed).** `/sites/[id]` on mobile is now reserve-first: drawer opens expanded (scrim framing), single-day (today) preselected, first available seat **pair** preselected (walks `pair`+`pairedBy`; `SunbedGroup` supersedes), map opens at seat-level zoom 20 (not fit-to-bounds parcel view) → one tap to Reserve. Kept the drawer (rejected inline rewrite). `manuallyExpanded`/"no scrim on first load" reversed per user. 344 unit tests (12 new in `sunbed-preselection.test.ts`). Deferred: recenter map on preselected pair. | 2026-07-09 |
| [008](008-employee-model.md) | Employee Model — staff attribution & till | **Done & LIVE on test+prod** (`dfc0512`…`2005640`, deployed 2026-06-20). All 4 phases: Employee roster + per-worker auto-attribution + per-worker till/close (`TillClose`) + manager monthly per-employee cash card (`getStaffTill`). **Full Alonso "Group A" closed.** Additive migration `20260619080811_add_employee_attribution` applied to TEST+PROD. partner unit green, docs/wiki synced. | 2026-06-20 |
| [009](009-anonymous-equipment-rentals.md) | Anonymous Equipment Rentals | **Done & LIVE on test+prod** (`29df253`…`97ae532`, deployed 2026-06-20). All 6 phases (0–5): faithful anon-mirror onto equipment rentals — book / pay / re-find / cancel without login + confirmation/reminder/cancellation emails. Additive migrations `…_rental_booking_anon_fields` + `…_rental_booking_reminder_sent_at` applied to TEST+PROD. user 323 unit + 45 integration, data 214 + 113 green. | 2026-06-20 |
| [007](007-operator-analytics.md) | Operator Analytics & Exports | **Done.** Alonso "Group B" shipped on the per-site accounting page: `@repo/data/analytics` (5 helpers) + 3 gated actions; rolling revenue lens (B2) + comp/occupancy (B1) + CSV export (B3). data 192 + 2 integration, partner 1427 green; pushed, preview `next build` Ready. ES/FI copy reviewed. | 2026-06-19 |
| [005](005-alonso-beach-model.md) | Alonso Beach App Model | **Done — all 7 phases.** Full functional model of the Alonso Beach competitor app in `.claude/alonso/` (6 subsystem docs + `synthesis-sunbnb.md` payload). Reference for Sunbnb manage-page/accounting design; floor-ops layer flagged as Sunbnb's biggest gap. Wiki promotion deferred (promote on use). | 2026-06-17 |
