# Track Registry

The control plane — every track, its state, and where to resume. **Start here.**
Maintained by the `create` and `handoff` workflows.

## Active

| ID | Track | Next action | Worktree | Updated |
|----|-------|-------------|----------|---------|
| [023](023-custom-whitelabel-pages.md) | Custom whitelabel pages — bespoke brand experiences for key customers | **P1 DONE 2026-08-21 (uncommitted, green); D1–D3 decided.** Today `/s/[slug]` reads five brand fields — a theme, not a brand experience. **The constraint that decides the design: the branded page is the BOOKING FUNNEL**, so any artefact that cannot mount the booking engine (uploaded HTML, an iframe) is disqualified however much design freedom it offers — shape is **custom shell, shared engine**. **D1: the artefact is a per-site React module in THIS REPO** (git is the versioning; price is that a copy tweak is a deploy, and the block-document tier is superseded not deferred). **Two gates**: a static registry entry keyed by **`Site.code`** — not the partner-editable slug, whose rename would silently orphan a page ([[track:022]]'s identifier, second use) — plus a `customBrandEnabled` column switched in admin, giving a deploy-free kill switch and merged-but-dark staging. **D3: brands live in `apps/user/brands/<name>`** (outside `app/`, so the router never walks it), loaded through a static `Site.code → () => import(…)` thunk map — laziness comes from the IMPORT SHAPE, not the location, so `packages/brands/*` was equivalent on that axis and was rejected on the boundary question instead: **a brand reaching into app internals is a FEATURE**, since isolation buys nothing against an adversary that does not exist and pushes shells toward re-implementing what they cannot import (no eslint boundary rule either; the residual coupling is answered by per-brand smoke tests, not walls). Footguns recorded: a template-literal import path cannot be code-split, and a barrel re-export undoes the map; Tailwind must scan `./brands/**`; chunking to be CONFIRMED at build, not assumed. **Fall back to the standard page, never an error**. Preview is free (Vercel preview on `main`). **D2: the brand tab exposes a field IFF that field is still in effect** — tokens go read-only under a live custom page (a bespoke shell decides its own look), **the slug stays editable** because it is the address not presentation (and a rename is safe precisely because the registry keys on `Site.code`), and the fallback case keeps everything editable since the standard page is what renders. Consequence: `saveBrand` needs a FIELD-level guard — it writes slug and tokens in one call, so rejecting the action wholesale would take the slug with it — and the guard must live in the action, since a hidden form still posts. Open Q9: if `generateMetadata` keeps building title/description from `brandName`, that field is still in effect and stays editable by the same rule. Three surfaces need the same answer, so a pure **manifest** of branded site codes goes in `@repo/data` (registry test-pinned against it) and admin reports *live* vs *awaiting code*. **Prerequisite nobody can skip: extract the booking engine from `SiteView` into a component with a stable prop contract**, else 'custom page' means N copies to re-check on every drawer/payment change. Findings: root CLAUDE.md's "brand page not persisted" is STALE; `bg_color`/`fg_color` ARE set on all three local branded sites (`brisa-marina` carries a custom `#b9e1ef` and renders visibly branded) — an earlier note claiming otherwise came from a query that never selected the two fields the page reads; only `primary_color`, `accent_color`, `logo_url` and `bg_image_url` are null, and none is consumed by `SiteViewBrand`. **P1+P2 SHIPPED. P1**: `Site.customBrandEnabled` (migration `20260821080420_add_site_custom_brand_enabled`, local + `sunbnb_test`), sudo-gated `setCustomBrand` (+11 tests — non-booleans REFUSED not coerced, `false` tested as carefully as `true` since the kill switch is half the point, both sudo cases assert nothing is written), and a Custom brand toggle in the admin sites table. Nothing reads the column yet, so behaviour is unchanged by construction. NOT browser-verified (admin sits behind a sudo login); column confirmed readable through the regenerated client. admin 193u, user 628u, partner 2046u, lint + tsc + `migrate:check` clean. **P2**: the funnel is now ONE component — `components/booking/BookingSurface.tsx` (`{ site, apiKey, theme? }`) owns the sticky sidebar, fixed drawer, scrim, minimise pill, on-mount bootstrap and `ReservationView` mount; `SiteView` 403 → 266 lines. `theme` is deliberately two colours, not `SiteViewBrand`. Peek arithmetic extracted to the pure `app/sites/[id]/peek-height.ts` (+6 tests, previously untested, phone-only failure mode); dead `withHours` Tabs branches deleted rather than moved. **Browser-verified and the DOM confirmed the extracted maths** (collapsed drawer `translateY(calc(100% - 118px))`, spacer `134px` = 66+52 and +16), branded page paints `bg_color` through the new `theme` prop, zero console errors. user 634u, tsc + lint clean. **P3**: `apps/user/brands/` (registry + `BrandMount` client boundary + contract + a plain `reference` module), `@repo/data/brand-manifest` (`resolveBrandRender` — the ONE answer to "what does a guest see", shared by all three apps), the fork + fall-back in `/s/[slug]`, admin module-select with a resolved *live / off / no module / unknown key* badge. **TWO EARLIER CLAIMS CORRECTED BY BUILDING**: (1) `Site.code` cannot key the registry — codes are minted per DATABASE, so the same venue differs across dev/test/prod and a committed registry would resolve nowhere else, including the Vercel previews that run on the TEST DB; replaced by `Site.customBrandKey` (key in code, pointer per environment, one module can serve a chain). (2) The import SHAPE alone does not code-split in the App Router — measured with two brands, a bare thunk awaited in a server component AND `next/dynamic` called from server code both put every brand in `/s/[slug]`'s page chunk; only a `dynamic()` reached through a CLIENT boundary emits one chunk per brand (verified: two brands, two chunks, page chunk clean). A source-check test guards it. Q9 answered: the shell owns layout and copy, not metadata, so `brandName`/`tagline` stay in effect and stay editable. user 642u, data 486u, admin 202u, `next build` clean. Local `brisa-marina` is switched to the reference module for eyeballing. **P4**: the brand tab follows the effective render — `saveSlug` split out of `saveBrand` (field-level guard: rejecting the action wholesale would take the address down with the appearance; the coverage-contract meta-guard caught the unregistered action immediately), `saveBrand` refuses token writes only while a bespoke page is LIVE and still accepts them in both half-configured states (the standard page renders there, so the tokens are genuinely in effect), and the tab shows a statement-of-fact panel keeping the slug. **Found en route: the Business-plan gate swallowed the panel** — a STARTER partner with a live bespoke page was shown an upgrade pitch for a branded page they already had, and lost the slug control D2 guarantees; the gate is now skipped for bespoke sites. Also deleted a FALSE user-facing banner ("preview only … won't be saved" — `saveBrand` persists) and the matching stale line in root CLAUDE.md. partner 2056u, tsc + lint clean; NOT browser-verified (partner is behind a login). Panel copy is a first draft and wants a founder review before promote. **P5 DONE — customer #1 is Alcúdia** (`brands/alcudia/`, browser-verified desktop+mobile): the bathymetric-descent page (sections step deeper between depth-contour separators; the booking panel is the constant raft), live data throughout (a source test rejects hardcoded euro amounts), the cover photo's CC BY credit housed in the footer, mobile CTA opens the real drawer via the store. Own chunk at build; page chunk clean. **Engine fix that fell out: the map opened on the seats' bbox centre — open WATER on a curved bay, all seats culled; now anchors on the nearest real seat (`inventoryAnchor`, +4 tests)** — first proof of the D3 bet that a real brand page finds engine defects a compact demo never would. ALL FIVE PHASES DONE. **Next: founder review of `/s/alcudia`; USER OPS `migrate:test` (3 migrations queued).** | — | 2026-08-21 |
| [020](020-large-venue-scale.md) | Large-venue scale — dozens of parcels, thousands of sunbeds | **PAUSED 2026-08-15 at a clean seam — P0+P1+P3(slice 1) DONE, committed local `main`, unpushed; founder confirmed "drag and rotate work now" on the live editor.** P0 harness (`sunbnb_scale` DB, deterministic seeder + EXPLAIN bench, guard unit-tested 17); P1 three additive indexes — booking conflict-guard (inside `FOR UPDATE`) **256ms → 0.98ms (261×)**, revenue-month 87×, overlap 84×; premise corrected: benefit scales with TOTAL table size, so P1 is ordinary growth work. P3: all four O(N²) render passes dead (`pair-selection` extracted + oracle-tested; SunbedMarker memo + latest-ref). Two founder-found bugs fixed + browser-verified en route: drag-pan regression (AdvancedMarker portals children async — listeners must key on the ELEMENT, `32f3c53`) and the PRE-EXISTING rotation teleport (pool sentinels poisoned the rearrange centroid ×(60/66) per apply, compounding — anchor found at exactly (60/66)^10 × site coords; fixed + teleport guard + data repaired, `15734fc`). Numbers + rejected candidates: `020-scale-baseline.md`. **Resume: (1) USER OPS `npm run migrate:test` before any `main` push (hook enforces); (2) founder picks P4 (batch writes + moveParcel txn, contained) vs P2 (set-based availability — bigger win, money-adjacent); Q4 (rate-limit the public availability endpoint) may jump the queue as a security fix.** | — | 2026-08-15 |
| [022](022-short-qr-urls.md) | Short QR URLs — address-keyed POS entry | **BUILD + DOCS COMPLETE (P1–P4 + P5 docs), 2026-08-20 — uncommitted, green, browser-verified. Waiting on USER OPS only.** The printed POS card moves from two cuids (`/sites/{cuid}/pos/{cuid}`, 80 chars → QR v5 37×37) to `/q/S-K7M2X9/1-1-1` (35 → **v3 29×29**). `getPosContext` was verified to use `itemId` ONLY to reach `item.sunbedGroup`, so the unit address is a complete key (**0 placed seats without a group** in local/test/prod). **P1** `Site.code` — migration `20260820101055_add_site_code` (local + `sunbnb_test`), pure `src/site-code.ts` (+21), idempotent `backfill:site-codes:*`, minting on BOTH partner creation paths (+7). **P2** `parseUnitAddress` beside the formatter it inverts (+15) + `app/q/resolve.ts` `resolveQrTarget` (+15 unit, +8 integration on the REAL unique index). **P3** `/q/[site]` + `/q/[site]/[unit]` rendering the EXISTING `PosView`s, legacy routes redirecting with a **deliberate fallback** (unconditional would send every fielded card to a dead URL wherever `Site.code` or the 021 address is missing — i.e. production), one shared `posAvailability` tail (+19). **P4** `inventory/qr-url.ts` builds the card URL by unpacking `seatLabel` — no payload change, keeping 020 C2's cut — and falls back rather than refusing to print (+6). **P5 docs**: CLAUDE.md tree + wiki ingest (one patch to `entities/reservation.md`; `physical-identity` queued under Planned with a hold until 021 lands — a page today would document a moving model). Findings: real data sits at **parcel 0 / row 0**; the four-part key is asserted because **`seq` restarts in every row** (defect injected — 2 fail, no collateral); a `redirect()` answers **200 with `;307;` in the RSC payload**, not a 307 header. Open: **Q1 defaulted to per-SEAT cards** (reversible in one line); Q3 rate-limiting and Q4 surfacing the code in the partner UI untouched. data 478u/378i, partner 2046u, user 628u/111i, lint + tsc clean. **USER OPS: (1) `npm run migrate:test` then `backfill:site-codes:test`; (2) PRINT A REAL SHEET AND SCAN IT — every QR-version claim here is arithmetic and the deliverable is physical; (3) production waits on [[track:021]].** | — | 2026-08-20 |
| [018](018-reservation-state-machine.md) | Reservation state machine — explicit model + matrix enforcement | **P0 DONE — de facto machine extracted** (`018-state-machine-defacto.md`: 7 state components, kinds K1–K7, full transition inventory, divergences D1–D13 incl. B1 split non-conservation ×3 paths, kind-blind guards from `paid-in-cash` overloading, delete-on-abandon of occupied seats). **P1 review held — all forks decided** (till partition on split · credit notes for cash refunds · money-rows-never-deleted rule · departed re-seatable same day); intended model drafted in `018-state-machine-intended.md` (state tuple + I1–I7 invariants + full transition table, reject-by-omission). **P1 SIGNED OFF; ▶ P2 — slices 1+2 shipped:** pure model (`@repo/data/reservation-machine`, committed `608bc55`) + server interpreter (`reservation-machine-apply`: applyTransition with fact-computed conditions, I4 delete defense, till-partitioning split — B1 end-to-end regression green; 15 integration tests, data 318u+320i, uncommitted). Slice (b) meta-guard shipped: single-writer ratchet over all apps (36 legacy sites / 9 files, exact-equality shrink-only allowlist) — **scan discovered 2 writer files P0 missed** (frontdesk fossil D14, partner reservation-detail D15). **P4 slices 1 (`4c386e4`) + 2 (uncommitted) done.** Slice 1: all six red cells GREEN (B1 impossible by construction; ratchet 19→13; the money-inventing split recompute killed). Slice 2: **D14/D15 fossils migrated** (frontdesk regains the multiday cycle; parent-only writes deleted; allowlist 9→7 files), **new `undoDepartWalkIn`** (same-day undo + conflict recheck; matrix 19 cells), **cron structurally can't delete money rows** (I4 filter). Partner 1966u+198i, data 320u+327i. **Slice 3 (UI) shipped (uncommitted):** bed-state = presentation shell over deriveState (grid + guards share ONE derivation); ↩ Undo-departure button in the FREE panel; unreserve dialog shows the truth (Seat → partitioned share, Group → actual till total). 2 i18n keys × 3 locales (**ES/FI await founder copy review**). Slice 3 committed `062f6e6` + **BROWSER-VERIFIED end-to-end** (partitioned €8.50 dialog, undo-departure re-seat, first real credit note `PARTNER-CN-2026-00001`, till void+recreate trail — screenshots + DB proof, rows cleaned). **Slice (e) shipped (uncommitted): collect flow on the machine — D5 + D6 dead at the action level** (settled walk-ins machine-rejected; abandon can never free a bed — paid race honestly resolves as pay.confirm; provider failures revert to unsettled cash). Interpreter gained the payment-rail executors (amountFromDb/mintAnonId/mollieCreate/reverifyOnce/mollieCancel/invoiceOnline); ratchet 13→8. **BUILT SCOPE COMPLETE (P0–P4, one day, 10 commits `608bc55`…HEAD; slice g uncommitted).** The signed transition table + interpreter is the ONLY reservation state writer (allowlist 36 sites/9 files → 9/2); B1 double-charge family + D5/D6/D14/D15 fixed by construction and browser-verified (first live credit note `PARTNER-CN-2026-00001`); wiki page `subsystems/reservation-state-machine.md` + CLAUDE.md tree synced. **Pushed to `main` 2026-08-11 (`be954d4`, migrations live on test DB). Founder op: ES/FI copy review before promote (4 keys).** Backlog: manage create-verbs batch (8→0), rentals/tables machines on the same rails, reservation-day.ts unification. | — | 2026-08-11 |
| [016](016-day-anchored-till.md) | Day-anchored till (per-day till the operator expects) | **FUNCTIONALLY COMPLETE & browser-verified (committed 2026-07-23: 'Day-anchor the per-worker till', local main).** Till reworked to day-anchored two-bucket windows (today + amber carry-over), sweep-together close via shared `closeEmployeeTill` (snapshot records carry-over), daily summary + day close lead with close-independent daily accumulation. data 265u+289i, partner 1897u+179i green. **Next (user ops): `migrate:test` before `main` push (hook enforces); founder copy-review of new en/es/fi keys before promote.** | — | 2026-07-23 |
| [013](013-settlement-ledger-till.md) | Settlement ledger — offline cash till source | **Building (P0a).** Replace the operational-status-derived till with an explicit cash-settlement ledger (`TillEntry`): a "Settle" cash event (alternative to card "Collect payment") is the till source, decoupled from check-in churn / seat turnover. Walk-in just occupies; Rent→Walk-in/Check-in rename; refund = "money returned" checkbox voids the settlement. Additive schema + cross-app. | — | 2026-06-21 |
| [001](001-knowledge-store.md) | Knowledge Store | Phase 1 — stand up Vercel test-env log-drain adapter | — | 2026-05-20 |
| [002](002-table-reservations.md) | Table Reservations | P1 complete + fully shipped (`71c75e7` on all branches). **▶ P1.5 QR order & pay at the table (dine-in tabs)** — design locked 2026-07-18 (tab opens on first order; v1 on site Order/Product rails; pay-at-end, one payment grouped by `paymentRef`). **Phases 2+3 DONE, green & committed (`b55f0b8` data + `f628d7a` user, local `main`, unpushed)** — data layer (`TableTab` + `openTableId` guard + `calculateTabTotal`/`processConfirmedTabPayment`) + user-app ordering (`/sites/[id]/dine/[tableId]` menu → `placeTabOrder` rounds → running tab; 28 `Dine` i18n keys await founder copy-review). data 265u+268i, user 398u+70i green; migration applied local+`sunbnb_test`, **`migrate:test` pending before `main` push** (pre-push hook enforces). **Next after reboot: phase 4 tab payment** ("Close & pay" + Mollie route + webhook branch + demo) | — | 2026-07-18 |
| [004](004-partner-test-architecture.md) | Partner Test Architecture | **PARTNER SCOPE COMPLETE** (Phases 0/3/1/4 — spine, fixes, backfill, cleanup+coverage-ratchet; 901 unit + 89 integration green, full gate green). Phase 1+4 local (`d4621b0`→`8386e7b`, 6 unpushed). Deferred: @repo/test-utils extraction, createWalkInRental race. | — | 2026-06-17 |
| [006](006-alonso-staff-ui.md) | Alonso → Staff UI Migration | **⚠ REOPENED 2026-07-09 — customer "literal parity" feedback.** Floor-state half stays at parity (P1–P8 + multiselect + refund + dark/zoom, live). Analysis found **three reporting/attribution *presentation* gaps** (plumbing exists, views missing) = new **P9**: (1) no today-revenue / total-rented hero on the manage floor; (2) till is aggregate-only — no per-seat+time itemized list, single-worker, cash-scoped vs the wanted full rental attribution; (3) accounting lacks a per-day *table* + surfaces invoice-count not sunbeds-rented/day. Cross into [007](007-operator-analytics.md) (gap 3) + [008](008-employee-model.md) (gap 2). **P9 BUILT 2026-07-10 (uncommitted, tests green):** all three gaps landed on the **accounting tab** (desktop, session-gated) on a **takings** basis — `@repo/data` `getReservationDayStats`/`getEmployeeShiftItems` (additive, no migration) → `getOperationsTrend`/`getStaffShiftItems` actions → Hoy card + *Historial detallado* table + expandable staff-till itemization. Data 241u+172i, partner 1716u green. **Next: browser-verify + commit; mobile staff-phone surfacing deferred.** | — | 2026-07-10 |
| [010](010-floor-reservation-lookup.md) | Floor Reservation Lookup & Arrivals — "Guests" host stand | **P1+P2 SHIPPED & LIVE on test+prod** (`e7952fd`). `findReservations` + the Guests bottom sheet (arrivals + name/phone/email search beyond today's grid, 🔍 anchor FAB, Locate→`BedDetail`, inline Check-in/Rent). **Floor-lookup gap vs Alonso closed end-to-end.** P3 **correctness slice** (error/empty/loading/a11y) shipped (`80bacc5`). **Next action: VALIDATE, don't build — the surpass layer (P3 roster-CRM, P4 scan-the-pass, P5 resell) is parked as unvalidated demand (Alonso-model hypotheses, not operator asks). Instrument the hypotheses (pass-QR usage, unfulfilled-hold rate) or wait for an operator signal before building.** | — | 2026-06-20 |

| [015](015-cash-sale-receipts.md) | Cash-sale receipts (invoice every income) | **ALL 5 PHASES BUILT & GREEN; backfill applied to test+prod (38 prod receipts).** Cash sales now get a PARTNER receipt (receipt-only, no commission), reusing the invoice core — P1 core (`skipCommission`/`invoicedAt`/`skipEmail` opts) · P2 rentals/orders · P3 wired at *settle* (guard=`recordCashSettlement`) · P4 env-tiered idempotent backfill · P5 **accounting-export UI** (fiscal summary + VAT-by-rate + CSV invoice register, replaced the detail tables). data 247u+219i, partner 1725u green. **Next (user ops): commit + deploy, then re-run `backfill:cash:production` to sweep the deploy gap.** Deferred: cash refund→credit-note. | — | 2026-07-10 |

## Proposed / backlog

| ID | Track | Why deferred | Updated |
|----|-------|--------------|---------|
| [024](024-card-present-payments.md) | Card-present payments — Viva as the card rail, and the floor app that hosts it | **Provider CONFIRMED Viva 2026-08-29; awaiting vendor answers + a demo-env spike before code.** Today the floor takes cash (till) or shows a Mollie QR the guest pays on their own phone — nothing for a guest standing at the parasol holding a card. **Founder's decision: Viva.com as the payment provider; native app in its own repo based on the web app; keep Mollie for online now, card on Viva, replace Mollie later if it proves out.** SumUp wins the narrow card-present comparison (1,49% vs 1,69%, and a browser-launchable URL scheme that ships card acceptance with ZERO native code) — and loses on the question that decides it: **SumUp has no marketplace/split-payment product, so it can only ever be a second card-present-only rail**, i.e. permanent two-acquirer friction per venue. Viva is a licensed bank with real-time split payments + eKYB sub-merchant onboarding, matching the agent model [[track:003]] exists to satisfy — so it buys a single-provider end state. **Late finding that qualifies the plan: Viva appears to have NO embeddable Tap-on-Phone SDK** — payment hands off to the separate Viva.com Terminal app, which undercuts "stay in our own UI", the main stated rationale for going native (Semi-Unattended / ECR-Controlled modes may soften it to a brief automatic switch). Their **Cloud Terminal API** may ship card from the EXISTING web app, decoupling the native app from this track entirely. Also established: payment collection is **not** confined to sunbed management (`restaurants/[id]/orders/actions.ts:163` `settleRestaurantTabCash` + the site orders dashboard), so the floor surface unifies manage + orders; `frontdesk` is session-gated and stays with the portal. Collect seam is already clean — `CollectPaymentModal.tsx:24` takes an injected `CollectActions` triple and the card rail is a new binding + one machine cell (`reservation-machine.ts:277`). **Next: ONE conversation with Viva's ISV team — (1) does Tap-on-Phone render in a third-party app or always hand off, (2) Spain ISV rates + marketplace terms, (3) can Cloud Terminal API target a Tap-on-Phone device. Ask Mollie the same week whether Tap to Pay reaches Spain for platform-connected accounts — if yes it beats the Viva path with zero migration.** **D1 decided: React Native, own repo, synced by copying** (iOS+Android portability) — which reuses *logic*, not JSX/Tailwind, and **cannot call Next server actions at all**, promoting an HTTP layer for the floor's ~40 gated actions to a hard prerequisite (P6.5); mitigation is copy the UI but consume `@repo/data`'s client-safe modules as a dependency so the state machine stays inside the single-writer ratchet + auth matrix. Viva being app-to-app rather than SDK-embedded means the RN app needs almost no native bridging. Open: **commission on card is still unresolved** (cash forfeits it today). | 2026-08-28 |
| [021](021-physical-unit-model.md) | Physical unit model — durable mount point, policy split from structure | **Scoped 2026-08-16, awaiting go-ahead — the foundation [[track:019]]'s device binding needs.** Founder's call: binding hardware to seats builds on a shaky base. Verified findings: **six** seat-deletion paths and **none knows devices exist** (`DeviceSeat.itemId` is `onDelete: Cascade`, and a zero-seat device is declined → deleting a parcel silently ambers a batch of parasols with no record of what they were bound to; 020's bulk delete makes that ONE transaction). The seat label's unit number is a **positional ordinal** recomputed site-wide, so a label is a coordinate not a name — killing label-based recovery. `SunbedGroup` is genuinely **1-n** (the pair assumption lives in `@unique pairId` + the grid generator; dev DB already has groups of 3 and 5), its ids are **already stable** under rotation/move (020's test pins them byte-identical across a 200-seat rotation), and it already carries `siteId` — so device discoverability comes free. Plan: **P0 audit** (are today's 3/5-member groups physical or commercial? are labels physically painted? does prod agree `pairId` is dead?) → **P1 retire the vestigial `pairId`** (dev has ZERO items with a pairId and no group; both expansion helpers already call SunbedGroup "authoritative" and reach pairId only under "Legacy fallback" — so this is a cheap FIRST step that spares every later phase a dual-write) → P2 persisted unit number (behaviour-preserving, fixes label drift on its own) → P2 universal units (null `sunbedGroupId` means *pool*, nothing else) → P3 identity survives re-pairing → P5 device binds to the unit + Restrict + binding log → P6 sell policy on the site→parcel→unit cascade (group membership stops implying joint booking) → P7 optional rename. **Device binding model settled 2026-08-16: devices SELF-REGISTER on first poll, partner assigns a seat code (location) from the fleet UI, assignment returns in the telemetry response.** Not a seat list, not a unit id, not an address in firmware — the location is the domain identity ("a parasol has no intrinsic identity; where in the parcel to find a party does"), while the URL key stays the immutable device code (the URL is potted). 019's telemetry persistence becomes a prerequisite and its P4 stops being a QR field flow. Flashing model decided: PERMANENT = 6-char device code (identity in every request — the fielded URL shape is unchanged) + customer number (bulk reflash on return); PROGRAMMABLE = site · parcel · row · position. **Supersedes 019's Q1** (bind to seats, because "SunbedGroup is a booking fact") — this track removes that premise by making the unit physical and moving policy to parameters. **P0 partly answered 2026-08-16 (read-only prod+test aggregates): `pair_id` with no group = **0 in dev, test AND production** → P1's drop is unblocked; production is 100% grouped in PAIRS with **zero placed-but-ungrouped seats**, so I1 already holds there and P3's backfill is a no-op (test: 24). The 3/5-member groups exist **only in dev** — check whether they're experimentation artifacts before treating n-ary grouping as a live domain case. Next: finish P0 (are labels physically painted?), then P1; answer Q1/Q2 (device↔unit cardinality, LED segment order) before P5.** | 2026-08-16 |
| [019](019-hw-api.md) | HW API — device state endpoint + telemetry | **REPLANNED 2026-08-16 — server work first, hardware last.** Binding model replaced by [[track:021]] P5 (devices self-register; partner assigns a location; config rides the poll response). Own phases now: **P5a device model + telemetry persistence** (next, no hardware needed) → **P5b fleet UI** → **P4 assignment + identify** → **freeze + bring-up**. Cross-track ordering in 021's *Delivery sequence*. Original scoping: **Scoped, ready to build — not deferred, waiting on hardware in transit.** Serves the parasol-mounted device (sibling repo `../sunbnb-hw`, design converged over 9 ADRs) from this backend with **no new infrastructure**: `GET /api/hw/{code}/state` + `POST …/telemetry`, polled directly over HTTPS by an ESP32-C6 (ADR 0008 deleted the broker/worker/gateway layer the earlier LoRa design implied). **Renamed off "beacon" 2026-08-13** — the device is a Wi-Fi *polling client* with an LED bar, not a broadcaster, and the name had leaked into a fielded, effectively unrenameable surface; `/api/hw/` names the client class, so a hardware or branding pivot never touches a potted URL. Wire contract drafted (4 closed states FREE/RESERVED/OCCUPIED/UNAVAILABLE, per-seat array + aggregate, **frozen seat order**, server-driven `pollAfterSec`, `cmd: stow`, **uniform 401** — the drafted 401-vs-404 split was an enumeration oracle against a code printed on a public beach) — **not frozen; cheap to change until devices are potted, expensive after**. Hard constraint: the route is a **second shell over `deriveState`** ([[track:018]]) — never a second opinion on seat state; fail-safe direction is *never resolve doubt toward FREE* (a wrong FREE double-sells a lounger). **Identity decided (Q2):** public 6-char Crockford-base32 code (server-issued, random) + secret 32-byte per-device token stored sha256-only; MAC-derived and sequential codes rejected. **Q1 DECIDED: explicit seat list** (`DeviceSeat` join table, `position` = mount order) — the binding is a *physical installation* fact and `SunbedGroup` is a *booking* fact; deriving one from the other over-scopes wherever grouping ≠ shading and lets an inventory edit silently change what a light means. **P1 CODE COMPLETE (uncommitted): `apps/user/app/api/hw/[code]/state/route.ts` + 43 route tests, user 521u green, tsc/lint clean.** Non-obvious finding baked in: the route must NOT fall back to the parent `operationalStatus` when today's `ReservationDay` row is absent (the manage page never hits this — it lazy-upserts; a device polls first, and upsert-on-read would be ~1.3 M writes/day), else a multiday guest who checked in yesterday reads OCCUPIED instead of RESERVED. **Next action: bring-up when the kit lands** — set `HW_TOKEN`/`HW_DEVICE_MAP` on test, curl the route, point the device; then P2 `Device`+`DeviceSeat` migration and **freeze the wire contract** (Q6 check character + Q7 token rotation must be answered first). **Device management split into three surfaces with three owners** (2026-08-13 restructure — the old roadmap conflated them): P3 **minting** = platform-side bench *script*, not a UI; P4 **binding** = operator-side *field* flow on the mobile/token-gated `/sites/[id]/manage` (binding happens at the parasol, not at a desk; needs `cmd: "identify"` to self-verify) — the surface Q1's explicit seat list makes necessary; P5 telemetry persistence + operator health list. Optional P1.5 telemetry stub for firmware bring-up. 💤 P6 fleet scale (Q3: ~1.3 M invocations/day at 1 500 devices). None of P3–P5 is needed for the demo — they start at the second and third unit. **Scope narrowed to the device↔API link; parked:** guest QR entry route + seat-state unification (the latter records a real POS bug — an unfiltered `reservations` include makes *canceled* bookings read as "not available"). | 2026-08-13 |
| [011](011-group-multiselect-reservation.md) | Group multiselect Reserve/Rent into one reservation | **Scoped, ready to build (awaiting go-ahead).** Partner manage multiselect Reserve/Rent currently makes one reservation PER SEAT — N bookings for one party on N loungers. Fix: a grouped create over the existing `reserveWithConflictGuard({ itemIds })` primitive (already used by the consumer flow + pair-expansion) so a multiselect = one reservation. **Decisions locked: always group (no toggle) + all-or-nothing (one taken seat fails the group).** Small, partner-only, **no schema/migration** (Reservation↔items already many-to-many; downstream already handles multi-item reservations). **Next: P1 — grouped action(s) + gated-actions/auth-matrix registration.** | 2026-06-20 |
| [012](012-multiday-per-day-operational-state.md) | Per-day operational state for multiday sunbed reservations | **Scoped + designed (awaiting decisions before P0).** Multiday bookings carry ONE operational state for the whole stay → stale day-1 arrival time, no daily expected→arrived reset, and a **day-2 double-sell** (departing day-1 frees the bed via `bed-state` `departed` filter). Fix: per-day `ReservationDay` rows (status/checkedInAt/departedAt per civil date); manage/frontdesk read today's row. **Schema change** (additive `ReservationDay` + `Site.timeZone` — no tz field exists today; `Restaurant.timeZone` is precedent). Bug is **duplicated in `frontdesk/`** (P3). Availability stays whole-stay (P2 invariant). **Next: confirm Open decisions Q1–Q5, then P0 expand migration.** | 2026-06-20 |
| [003](003-stripe-connect-compliance.md) | Stripe Connect compliance | **Green-field — consumer Stripe code removed (2026-05-23).** Stripe is subs-only; consumer = Mollie + Demo. Build on Connect from scratch if/when consumer Stripe is reintroduced. | 2026-05-23 |

## Done / archived

| ID | Track | Outcome | Updated |
|----|-------|---------|---------|
| [017](017-venue-timezone-anchoring.md) | Venue timezone anchoring — one civil-day convention platform-wide | **Done — all 7 phases (P1–P7).** Every date boundary now anchors to the venue civil day: settlement final-day (P1), reminder/availability windows (P2), reservation writes (P3), accounting/VAT month windows (P4), analytics bucketing incl. the occupancy >100% double-count (P5), `Site.timeZone` auto-derive+backfill+restaurant inheritance (P6/P7); plus a west-of-UTC `localMidnight` fix and the restaurant `+24h` DST fix. Primitives live in `@repo/data/site-day`. **P1+P2 pushed 2026-08-11; P3–P7 committed local `main` (11 commits `a8fbc07`…HEAD), unpushed; no schema change → no migration. User ops: push/deploy + run `backfill:tz:*`.** | 2026-08-13 |
| [014](014-reserve-first-site-view.md) | Reserve-first site view (user app) | **Done & user-verified (local commit, unpushed).** `/sites/[id]` on mobile is now reserve-first: drawer opens expanded (scrim framing), single-day (today) preselected, first available seat **pair** preselected (walks `pair`+`pairedBy`; `SunbedGroup` supersedes), map opens at seat-level zoom 20 (not fit-to-bounds parcel view) → one tap to Reserve. Kept the drawer (rejected inline rewrite). `manuallyExpanded`/"no scrim on first load" reversed per user. 344 unit tests (12 new in `sunbed-preselection.test.ts`). Deferred: recenter map on preselected pair. | 2026-07-09 |
| [008](008-employee-model.md) | Employee Model — staff attribution & till | **Done & LIVE on test+prod** (`dfc0512`…`2005640`, deployed 2026-06-20). All 4 phases: Employee roster + per-worker auto-attribution + per-worker till/close (`TillClose`) + manager monthly per-employee cash card (`getStaffTill`). **Full Alonso "Group A" closed.** Additive migration `20260619080811_add_employee_attribution` applied to TEST+PROD. partner unit green, docs/wiki synced. | 2026-06-20 |
| [009](009-anonymous-equipment-rentals.md) | Anonymous Equipment Rentals | **Done & LIVE on test+prod** (`29df253`…`97ae532`, deployed 2026-06-20). All 6 phases (0–5): faithful anon-mirror onto equipment rentals — book / pay / re-find / cancel without login + confirmation/reminder/cancellation emails. Additive migrations `…_rental_booking_anon_fields` + `…_rental_booking_reminder_sent_at` applied to TEST+PROD. user 323 unit + 45 integration, data 214 + 113 green. | 2026-06-20 |
| [007](007-operator-analytics.md) | Operator Analytics & Exports | **Done.** Alonso "Group B" shipped on the per-site accounting page: `@repo/data/analytics` (5 helpers) + 3 gated actions; rolling revenue lens (B2) + comp/occupancy (B1) + CSV export (B3). data 192 + 2 integration, partner 1427 green; pushed, preview `next build` Ready. ES/FI copy reviewed. | 2026-06-19 |
| [005](005-alonso-beach-model.md) | Alonso Beach App Model | **Done — all 7 phases.** Full functional model of the Alonso Beach competitor app in `.claude/alonso/` (6 subsystem docs + `synthesis-sunbnb.md` payload). Reference for Sunbnb manage-page/accounting design; floor-ops layer flagged as Sunbnb's biggest gap. Wiki promotion deferred (promote on use). | 2026-06-17 |
