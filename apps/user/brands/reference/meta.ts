import type { BrandMetadata } from '../types'

/** The wiring demo says so here too, for the same reason the page does. */
export const metadata: BrandMetadata = {
  title: 'Reference brand module',
  description: 'Wiring demo for the bespoke brand mechanism — not a customer design.',
}
