import type { BrandMetadata } from '../types'

/**
 * What the tab, the search result and the shared link say (track 023 Q9).
 * The title is the name the page itself shows — anything else reads as a
 * different venue to someone who followed a link here.
 */
export const metadata: BrandMetadata = {
  title: "Platja d'Alcúdia",
  description:
    "Six kilometres of shallow water on Mallorca's north coast. Reserve a sunbed for the day — one price, every bed.",
}
