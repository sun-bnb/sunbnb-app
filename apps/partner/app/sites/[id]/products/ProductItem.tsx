'use client';

import React from 'react';
import { Product } from '@/types/shared'

export default function ProductItem({
  product
}: {
  product: Product
}) {
  return (
    <div>Product</div>
  )
}
